# @cleverforge/sanity-clever-forms-sanity

Sanity Content Lake repository adapter for the CleverForms server package.

Provide a server-side Sanity client with the minimum required read/write permissions, then pass the adapter to `createSubmissionService()`.

License: Apache-2.0
